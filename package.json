{
 "name": "github-repositories-filter",
 "version": "1.0.0",
 "scripts": {
  "client": "cd client && npm start",
  "server": "cd server && node ./bin/www",
  "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
 },
 "dependencies": {
  "express": "^4.16.2"
 },
 "devDependencies": {
  "concurrently": "5.2.0"
 }
}
